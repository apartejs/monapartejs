<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64">
  <!-- The favicon before JavaScript runs, and the source of favicon.ico: the
       silhouette, solid — the one drawing that survives 16 px. FaviconService
       then redraws it live with the state's colour and the theme's ground.
       Same house as the icons (MARK_PATH, checked by mark.spec.ts). -->
  <rect width="64" height="64" rx="14" fill="#17141c"/>
  <g transform="translate(4 2) scale(0.4667)">
    <path d="M60 4 L112 34 L112 84 L48 84 L30 100 L30 84 L8 84 L8 34 Z" fill="#d946ef"/>
    <text x="60" y="71" font-family="Georgia, 'Times New Roman', serif" font-size="40" text-anchor="middle" fill="#f6f2ea">('.')</text>
  </g>
</svg>
