---
name: Application bug
about: Something is broken in the interface, the download, files or artifacts
labels: bug
---

## What happens

## What should happen

## Environment

- Browser and version:
- GPU / `navigator.gpu` available:
- `crossOriginIsolated` in the console:
- Device (RAM, OS):

## If the model is involved

Paste the content of `/debug/prompt` (the last real exchange) — or enable traces with
`localStorage.setItem('bp.debug','1')` and reload.
