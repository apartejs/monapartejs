# Build / deps
/node_modules/
/dist/
/.angular/
/tmp/
/out-tsc/

# IDE / OS
.idea/
.DS_Store
Thumbs.db

# Logs / caches
npm-debug.log
pnpm-debug.log
.sass-cache/

# notes internes — jamais dans le repo public
docs/superpowers/

# Notes de terrain, jamais publiées : journal de friction avec le modèle,
# brouillons de brief pour la prochaine passe SFT. Vit à côté du code parce
# que c'est en développant qu'on constate, mais ce n'est pas de la doc.
*.local.md

# Retours d'audit et notes de cadrage Claude — local, jamais publiés
.claude/
